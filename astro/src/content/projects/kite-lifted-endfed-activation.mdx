---
title: "Kite-Lifted 40m End-Fed Activation: Field Deployment & Hard Lessons"
description: "Kite-lifted 40m end-fed activation on Coochiemudlo Island: mechanical load path, static management, and field behaviour."
pubDate: 2025-09-25
draft: false
tags: [hf, 40m, endfed, kite, portable, grounding, field-ops]
image: /images/projects/kite-activation/hero.jpg
---

Kiri (VK4KLB / VK7KLB) and I headed out to Main State Beach at Coochiemudlo to activate AU-8924 and AU-7027 together. It was September in Queensland, so 0600z translated to just after 4 pm local time. Proper afternoon sea breeze. Enough wind to make the long-standing “kite antenna” idea finally feel realistic rather than theoretical.


<figure className="heroFigure">
  <img src="/images/projects/kite-activation/hero.jpg" alt="Kite-lifted 40m EFHW at Coochiemudlo Beach" />
  <figcaption>Hero shot — kite up, wire out, beach in frame.</figcaption>
</figure>

<section className="postStats">
  <div className="statCard"><div className="statNum">42</div><div className="statLbl">QSOs Logged</div></div>
  <div className="statCard"><div className="statNum">37m 3s</div><div className="statLbl">On-Air Duration</div></div>
  <div className="statCard"><div className="statNum">AU-8924</div><div className="statLbl">Marine Park Ref</div></div>
  <div className="statCard"><div className="statNum">AU-7027</div><div className="statLbl">Marine Park Ref</div></div>
  <div className="statCard"><div className="statNum">~22 m</div><div className="statLbl">Peak Antenna Height</div></div>
  <div className="statCard"><div className="statNum">&gt;3 kV</div><div className="statLbl">Static Observed</div></div>
</section>

<section className="callout">
  <h2>Propagation Snapshot</h2>
  <p>Extracted from Ham2K Portable Logger — 42 QSOs in 37 minutes, including a 40 m path into Europe.</p>
  <figure>
    <img src="/images/projects/kite-activation/ham2k-map.png" alt="Ham2K map showing QSO distribution including Europe path" />
    <figcaption>Ham2K log map — Main State Beach into VK-land and Europe.</figcaption>
  </figure>
</section>

<section className="callout qsoDash" data-adif-a="/logs/2025-09-21-au-7027.adi" data-adif-b="/logs/2025-09-21-au-8924.adi">
  <h2>Log Summary</h2>

  <div className="qsoKpis" aria-label="QSO summary statistics">
    <div className="qsoKpi"><div className="qsoKpiNum" data-kpi="total">—</div><div className="qsoKpiLbl">Total QSOs</div></div>
    <div className="qsoKpi"><div className="qsoKpiNum" data-kpi="duration">—</div><div className="qsoKpiLbl">On-air duration</div></div>
    <div className="qsoKpi"><div className="qsoKpiNum" data-kpi="first">—</div><div className="qsoKpiLbl">First QSO (UTC)</div></div>
    <div className="qsoKpi"><div className="qsoKpiNum" data-kpi="last">—</div><div className="qsoKpiLbl">Last QSO (UTC)</div></div>
    <div className="qsoKpi"><div className="qsoKpiNum" data-kpi="bands">—</div><div className="qsoKpiLbl">Bands</div></div>
    <div className="qsoKpi"><div className="qsoKpiNum" data-kpi="modes">—</div><div className="qsoKpiLbl">Modes</div></div>
  </div>

  <div className="qsoGrid">
    <div className="qsoPanel">
      <h3>Band breakdown</h3>
      <div className="tableWrap">
        <table aria-label="Band breakdown table">
          <thead><tr><th>Band</th><th>QSOs</th><th>%</th></tr></thead>
          <tbody data-band-table></tbody>
        </table>
      </div>
    </div>

    <div className="qsoPanel">
      <h3>Contact rate</h3>
      <div className="qsoChartWrap">
        <canvas id="qsoRateChart" width="920" height="260" aria-label="Contact rate chart" role="img"></canvas>
      </div>
      <div className="qsoChartMeta" data-rate-meta>—</div>
    </div>
  </div>

  <details className="qsoDetails">
    <summary>Full QSO table (from ADIF)</summary>
    <div className="qsoControls">
      <label className="qsoControl">Filter: <input type="text" data-qso-filter placeholder="Callsign, band, mode…" /></label>
      <label className="qsoControl">Band: <select data-qso-band><option value="">All</option></select></label>
      <label className="qsoControl">Mode: <select data-qso-mode><option value="">All</option></select></label>
    </div>

    <div className="tableWrap">
      <table aria-label="Full QSO table">
        <thead>
          <tr>
            <th>UTC</th>
            <th>Call</th>
            <th>Band</th>
            <th>Mode</th>
            <th>RST S</th>
            <th>RST R</th>
            <th>Grid</th>
            <th>Ref</th>
          </tr>
        </thead>
        <tbody data-qso-table></tbody>
      </table>
    </div>
  </details>

  <div className="qsoError" data-qso-error hidden></div>
</section>

I’ve wanted to hang this 40 m EFHW under a kite for years. Not for drama, and not because it photographs well, but because I’ve always wondered how much of an antenna’s good behaviour comes from the fact that we force it to sit still. In the backyard this antenna is almost dull in its reliability. It’s tuned for 7.074 MHz when deployed as a sloper from roughly 9 m down to about 2 m. The transformer is a 56:1 autotransformer wound on an FT82-43 with an FT50-43 nested inside, built on the UniBalun PCB from DG1JAN. The wire is insulated SOTABeams. In a fixed deployment it sits comfortably under 1.5:1 SWR across the resonant bands and is practically 1:1 on 15 m. It behaves so predictably that you stop thinking about it.

Which made it the perfect candidate to disturb.

We chose our position carefully, well clear of anyone walking the beach. A long tensioned wire under wind load is not something you casually let drift over people. The Rainbow LS-17.5 Lifter Sled went up cleanly on Dyneema rated for 200 kg. Wind was under 15 kph with gusts below 25 kph — steady rather than erratic — and once it found height the antenna was sitting somewhere around 22 metres above the sand, sloping down toward the operating position.

<figure>
  <img src="/images/projects/kite-activation/in-air-1.jpg" alt="Kite at height with visible sloper angle" />
  <figcaption>Kite at height, wire angle obvious.</figcaption>
</figure>

<figure>
  <img src="/images/projects/kite-activation/in-air-2.jpg" alt="Operating position with sloper down to rig" />
  <figcaption>Operating position + sloper down to the rig.</figcaption>
</figure>

The antenna was tied into a loop in the kite string about two metres below the bridle attachment, with a short section of bungee between the kite line and the antenna wire. That small elastic section mattered far more than it looks on paper. Dyneema does not stretch, and without something to absorb gust energy every change in wind speed would transmit directly into the transformer and connectors. The rope carried the load. The antenna followed the slope. The RF hardware was left to do RF work rather than structural work.

The feedline chain was unchanged from my normal portable configuration. About 5.6 metres of RG58 CU ran from the transformer into a bleed resistor array made from five 1 MΩ resistors in parallel inside a sealed enclosure with SO-239 connectors. From there it passed through a ferrite choke, into the AT-100M Pro tuner, and finally into the FX-4CR. The ground strap from the bleed enclosure to a 1.3 metre steel ground rod was kept under 30 centimetres to keep that path as short as possible. Yes, the rod went into sand. That is what the beach provides.

Static built quickly. At one point I measured over 3 kV on the wire, which is the sort of number that makes you quietly grateful you didn’t treat the bleed network as optional. The resistors did exactly what they were designed to do and nothing made it through to the radio, but seeing that voltage on a moving airborne conductor is a useful reminder that wind and long wires generate charge very efficiently.

<figure>
  <img src="/images/projects/kite-activation/kit-layout.jpg" alt="Activation kit laid out on sand" />
  <figcaption>Kit laid out on sand.</figcaption>
</figure>

<figure>
  <img src="/images/projects/kite-activation/components-close.jpg" alt="Transformer, bleed box and choke close-up" />
  <figcaption>Close-up of transformer / bleed box / choke.</figcaption>
</figure>

> PHOTO PLACEHOLDERS: Additional close-ups you might want later — transformer enclosure, bleed array wiring, choke winding, kite attachment point, anchor stake under load.

We started calling at 0601z, just after 4 pm local time. The first entries in the log are VK4DTS and VK4CYA at 06:01:56z, both 59. By 06:05z VK2OKR, VK2BXB and VK2AUS were in the log with 55–56 reports. The band was open and cooperative.

The sloper angle shifted constantly between roughly 60° and 25° as the wind varied, and the SWR reflected that movement. There was no single stable resonant point to admire. The tuner spent the entire activation adjusting to a geometry that refused to stay put. Whenever a gust lifted the kite a few extra metres you could hear the tuner respond, and when the kite dipped the numbers shifted again.

At 06:13:29z F5PYI came in, 59 both ways on 40 metres. From a beach in Moreton Bay into France under a wire that was actively moving in the wind, which is the sort of contact that makes you look up at the kite and smile.

VK3PF appears in the log at 06:36:11z, also 59 both ways, and that was the QSO during which the kite came down. It didn’t drift gently; it simply lost lift and returned to the sand mid-contact. The rope took the load as intended and nothing tore out of the transformer. The heavy-duty dog stake anchoring the line had already been vibrating under sustained pull, and when the kite dropped everything relaxed in one go. Almost immediately a dog decided the bright nylon sled was the most interesting object on the beach and ran toward it with enthusiasm that suggested we had provided afternoon entertainment. We retrieved it, reset, and put it back in the air without drama.

<div style={{ position: 'relative', paddingBottom: '56.25%', height: 0, overflow: 'hidden', margin: '2rem 0' }}>
  <iframe
    src="https://www.youtube.com/embed/Fu6_WNSBXKg"
    title="Kite-Lifted 40m EFHW Activation Video"
    style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%' }}
    frameBorder="0"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
    allowFullScreen
  />
</div>

The final QSO in the log is at 06:39:00z. From 06:01:56z to 06:39:00z — just over 37 minutes — we logged 42 QSOs across AU-8924 and AU-7027. Despite the continuously changing angle, the wandering SWR, and the brief unscheduled descent, the system kept functioning. The tuner worked continuously, the bleed resistors handled static, the choke prevented RF from coming back into the operating position, and the antenna radiated consistently enough to produce solid reports, including the path into Europe.

Looking back over the log later, what stays with me isn’t a grand lesson about antenna theory. It’s simply that the whole thing behaved like a system rather than a single component. Mechanical decisions mattered. The bleed network mattered. The elasticity in the line mattered. The geometry never stayed the same for more than a few seconds, and yet the radio kept making contacts.

I’ll run it again.

VK4MPB / VK7MPB

<figure>
  <img src="/images/projects/kite-activation/kite-efhw-system.svg" alt="System overview diagram of kite-lifted EFHW" />
  <figcaption>System overview — kite-lifted EFHW deployment.</figcaption>
</figure>

<div className="sectionDivider" aria-hidden="true" />
---

## IF YOU WANT TO TRY THIS YOURSELF

<div className="fieldSheet">
  <div className="fieldSheetHeader">
    <div className="fieldSheetTitle">Field Spec Sheet</div>
    <div className="fieldSheetSubtitle">Build notes and expectations, formatted for quick scanning.</div>
  </div>

  <div className="buildGrid">

    <div className="buildCard">
      <h3>Antenna</h3>
<ul>
  <li>40 m EFHW tuned for 7.074 MHz (fixed sloper baseline ~9 m to 2 m)</li>
  <li>56:1 autotransformer</li>
  <li>FT82-43 core with FT50-43 nested inside</li>
  <li>Built on UniBalun PCB (DG1JAN)</li>
  <li>Insulated SOTABeams wire</li>
</ul>
    </div>

    <div className="buildCard">
      <h3>Radial</h3>
<ul>
  <li>Elevated radial: 7.8 m</li>
  <li>Height: ~1.5–2 m above ground</li>
  <li>Supported independently of the kite system</li>
</ul>
    </div>

    <div className="buildCard">
      <h3>Feedline &amp; RF Chain</h3>
<p><strong>Signal path:</strong></p>
<p>Antenna + elevated radial → Bleed resistor array → Ferrite choke → AT-100M Pro tuner → FX-4CR</p>
<ul>
  <li>5.6 m RG58 CU</li>
  <li>Bleed array: 5 × 1 MΩ resistors in parallel (200 kΩ effective)</li>
  <li>Sealed enclosure with SO-239 connectors</li>
  <li>Ground strap &lt; 30 cm</li>
  <li>1.3 m steel ground rod</li>
</ul>
    </div>

    <div className="buildCard">
      <h3>Kite &amp; Mechanical</h3>
<ul>
  <li>Rainbow LS-17.5 Lifter Sled (~1.6 m²)</li>
  <li>Dyneema line rated 200 kg</li>
  <li>Antenna tied ~2 m below bridle</li>
  <li>Short bungee section between kite string and antenna</li>
  <li>Heavy-duty dog stake anchor</li>
</ul>
    </div>

  </div>

  <div className="buildCard fullWidth">
    <h3>What to Expect</h3>
<ul>
  <li>Height will vary (ours peaked ~22 m)</li>
  <li>Sloper angle will continuously change</li>
  <li>SWR will move with wind and height</li>
  <li>A capable tuner is essential</li>
  <li>Static accumulation in coastal wind can exceed 3 kV</li>
</ul>
  </div>

</div>

Build it thoughtfully — mechanically and electrically — and it works. It just won’t sit still while it does.

73 for now, and thanks for the QSOs folks!

VK4MPB / VK7MPB

<figure>
  <img src="/images/projects/kite-activation/kite-efhw-system.svg" alt="System overview diagram of kite-lifted EFHW" />
  <figcaption>System overview — kite-lifted EFHW deployment.</figcaption>
</figure>

---

<style>{String.raw`
/* Page-specific width/whitespace overrides (keeps readability, reduces dead space on wide screens) */
:global(article){
  max-width: 1180px;
  margin-left: auto;
  margin-right: auto;
  padding-left: 18px;
  padding-right: 18px;
}

/* Some Astro themes wrap markdown in .prose (or similar) with a narrow max-width. Remove that constraint here. */
:global(article .prose){
  max-width: none;
}

/* Keep paragraphs readable, but wider than the default so the page doesn’t look “lost” in whitespace */
:global(article p),
:global(article li){
  max-width: 92ch;
}

/* Allow headings, figures, tables, and callouts to use the full article width */
:global(article h1),
:global(article h2),
:global(article h3),
:global(article h4),
:global(article figure),
:global(article .callout),
:global(article .qsoDash),
:global(article .fieldSheet),
:global(article .tableWrap){
  max-width: none;
}

@media (min-width: 1400px){
  :global(article){
    max-width: 1320px;
    padding-left: 22px;
    padding-right: 22px;
  }
  :global(article p),
  :global(article li){
    max-width: 102ch;
  }
}

.postStats{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(160px,1fr));
  gap:14px;
  margin:22px 0 30px;
}
.statCard{
  border:1px solid rgba(255,255,255,0.08);
  background:rgba(255,255,255,0.04);
  border-radius:16px;
  padding:14px 16px;
  backdrop-filter: blur(6px);
}
.statNum{
  font-size:1.4rem;
  font-weight:700;
  line-height:1.1;
}
.statLbl{
  opacity:.8;
  font-size:.9rem;
  margin-top:6px;
}
.callout{
  margin:32px 0;
  padding:18px 18px 14px;
  border-radius:18px;
  border:1px solid rgba(255,255,255,0.10);
  background:rgba(255,255,255,0.03);
}
.tableWrap{
  overflow-x:auto;
  margin-top:10px;
}
table{
  width:100%;
  border-collapse:collapse;
}
th,td{
  text-align:left;
  padding:10px 10px;
  border-bottom:1px solid rgba(255,255,255,0.08);
}
th{
  font-weight:600;
  opacity:.9;
}
figure img{
  width:100%;
  border-radius:14px;
}

.heroFigure{
  margin: 18px 0 10px;
}
.heroFigure img{
  width: 100%;
  max-height: 460px;
  object-fit: cover;
  border-radius: 18px;
}
.heroFigure::before{
  content: "";
  display: block;
  position: relative;
}
.heroFigure img{
  box-shadow: 0 12px 30px rgba(0,0,0,0.18);
}
.heroFigure figcaption{
  opacity: 0.85;
}

.qsoDash .qsoNote{
  margin-top: 6px;
  opacity: 0.85;
}
.qsoKpis{
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 12px;
  margin: 14px 0 18px;
}
.qsoKpi{
  border: 1px solid rgba(255,255,255,0.10);
  background: rgba(255,255,255,0.03);
  border-radius: 16px;
  padding: 12px 14px;
}
.qsoKpiNum{
  font-size: 1.35rem;
  font-weight: 700;
  line-height: 1.1;
}
.qsoKpiLbl{
  opacity: 0.80;
  font-size: 0.9rem;
  margin-top: 6px;
}
.qsoGrid{
  display: grid;
  grid-template-columns: 1fr;
  gap: 14px;
  margin: 10px 0 6px;
}
@media (min-width: 980px){
  .qsoGrid{ grid-template-columns: 0.9fr 1.1fr; }
}
.qsoPanel{
  border: 1px solid rgba(255,255,255,0.10);
  background: rgba(255,255,255,0.02);
  border-radius: 18px;
  padding: 14px 14px 10px;
}
.qsoPanel h3{
  margin: 0 0 10px;
}
.qsoChartWrap{
  border-radius: 14px;
  overflow: hidden;
  border: 1px solid rgba(255,255,255,0.10);
  background: rgba(0,0,0,0.18);
}
#qsoRateChart{
  display: block;
  width: 100%;
  height: auto;
}
.qsoChartMeta{
  margin-top: 8px;
  opacity: 0.80;
  font-size: 0.92rem;
}
.qsoDetails{
  margin-top: 14px;
}
.qsoDetails summary{
  cursor: pointer;
  font-weight: 600;
}
.qsoControls{
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  align-items: center;
  margin: 12px 0 10px;
}
.qsoControl{
  display: inline-flex;
  gap: 8px;
  align-items: center;
  font-size: 0.95rem;
  opacity: 0.95;
}
.qsoControl input, .qsoControl select{
  padding: 8px 10px;
  border-radius: 12px;
  border: 1px solid rgba(255,255,255,0.14);
  background: rgba(0,0,0,0.18);
  color: inherit;
}
.qsoError{
  margin-top: 12px;
  padding: 12px 14px;
  border-radius: 14px;
  border: 1px solid rgba(255,120,120,0.35);
  background: rgba(255,120,120,0.10);
}
  .sectionDivider{
    height: 1px;
    margin: 52px 0 46px;
    background: linear-gradient(
      to right,
      rgba(255,255,255,0) 0%,
      rgba(255,255,255,0.18) 20%,
      rgba(255,255,255,0.35) 50%,
      rgba(255,255,255,0.18) 80%,
      rgba(255,255,255,0) 100%
    );
  }

  .fieldSheet{
    margin: 18px 0 28px;
    padding: 18px 18px 20px;
    border-radius: 20px;
    border: 1px solid rgba(255,255,255,0.10);
    background: linear-gradient(180deg, rgba(255,255,255,0.05), rgba(255,255,255,0.02));
  }

  .fieldSheetHeader{
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    justify-content: space-between;
    gap: 10px;
    padding: 2px 2px 14px;
    border-bottom: 1px solid rgba(255,255,255,0.10);
    margin-bottom: 16px;
  }

  .fieldSheetTitle{
    font-weight: 800;
    letter-spacing: 0.2px;
    font-size: 1.1rem;
  }

  .fieldSheetSubtitle{
    opacity: 0.80;
    font-size: 0.95rem;
  }

  .buildGrid{
    display: grid;
    grid-template-columns: 1fr;
    gap: 18px;
    margin: 0 0 18px;
  }

  @media (min-width: 980px){
    .buildGrid{ grid-template-columns: 1fr 1fr; }
  }

  .buildCard{
    border: 1px solid rgba(255,255,255,0.10);
    background: rgba(0,0,0,0.08);
    border-radius: 18px;
    padding: 18px 18px 16px;
  }

  .buildCard h3{
    margin: 0 0 10px;
  }

  .buildCard.fullWidth{
    width: 100%;
  }

  .buildCard.fullWidth ul{
    margin-bottom: 0;
  }
`}</style>
<script>{String.raw`
(() => {
  const pad2 = (n) => String(n).padStart(2, '0');
  const fmtUTC = (d) => pad2(d.getUTCHours()) + ':' + pad2(d.getUTCMinutes()) + ':' + pad2(d.getUTCSeconds());
  const fmtDur = (ms) => {
    const s = Math.max(0, Math.round(ms / 1000));
    const m = Math.floor(s / 60);
    const r = s % 60;
    return String(m) + 'm ' + pad2(r) + 's';
  };

  function parseAdif(text) {
    // Minimal ADIF parser: extracts <TAG:LENGTH>VALUE pairs per record, split by <EOR>
    const upper = text.replace(/\r/g, '');
    const parts = upper.split(/<EOR>/i);
    const records = [];

    for (const part of parts) {
      const rec = {};
      const re = /<([A-Z0-9_]+)(?::(\d+))?(?::[^>]*)?>/gi;
      let m;
      let lastIndex = 0;
      let lastTag = null;
      let lastLen = null;

      while ((m = re.exec(part)) !== null) {
        if (lastTag) {
          const valStart = lastIndex;
          const valEnd = lastLen ? (valStart + lastLen) : m.index;
          const rawVal = part.slice(valStart, valEnd);
          rec[lastTag] = rawVal.trim();
        }
        lastTag = m[1].toUpperCase();
        lastLen = m[2] ? Number(m[2]) : null;
        lastIndex = re.lastIndex;
      }
      if (lastTag) {
        const rawVal = part.slice(lastIndex, lastLen ? (lastIndex + lastLen) : undefined);
        rec[lastTag] = (rawVal || '').trim();
      }

      if (Object.keys(rec).length) records.push(rec);
    }

    return records;
  }

  function toUtcDate(rec) {
    // ADIF: QSO_DATE=YYYYMMDD, TIME_ON=HHMMSS or HHMM
    const ds = (rec.QSO_DATE || rec.QSO_DATE_OFF || '').trim();
    const ts = (rec.TIME_ON || rec.TIME_OFF || '').trim();
    if (!ds || !/^\d{8}$/.test(ds)) return null;
    const y = Number(ds.slice(0,4));
    const mo = Number(ds.slice(4,6)) - 1;
    const da = Number(ds.slice(6,8));

    let hh = 0, mm = 0, ss = 0;
    if (/^\d{6}$/.test(ts)) {
      hh = Number(ts.slice(0,2));
      mm = Number(ts.slice(2,4));
      ss = Number(ts.slice(4,6));
    } else if (/^\d{4}$/.test(ts)) {
      hh = Number(ts.slice(0,2));
      mm = Number(ts.slice(2,4));
      ss = 0;
    }

    return new Date(Date.UTC(y, mo, da, hh, mm, ss));
  }

  function bandLabel(rec) {
    // Prefer BAND, else derive from FREQ
    if (rec.BAND) return rec.BAND.trim().toLowerCase();
    const f = rec.FREQ ? Number(rec.FREQ) : NaN;
    if (!Number.isFinite(f)) return '';
    if (f >= 1.8 && f < 2.0) return '160m';
    if (f >= 3.5 && f < 4.0) return '80m';
    if (f >= 7.0 && f < 7.3) return '40m';
    if (f >= 10.0 && f < 10.2) return '30m';
    if (f >= 14.0 && f < 14.4) return '20m';
    if (f >= 18.068 && f < 18.168) return '17m';
    if (f >= 21.0 && f < 21.45) return '15m';
    if (f >= 24.89 && f < 24.99) return '12m';
    if (f >= 28.0 && f < 29.7) return '10m';
    return '';
  }

  function modeLabel(rec) {
    return (rec.MODE || '').trim().toUpperCase();
  }

  function refLabel(rec) {
    // Try a few common fields (POTA/SOTA/other references). If not present, blank.
    return (rec.MY_SIG_INFO || rec.SIG_INFO || rec.MY_SIG || rec.SIG || '').trim();
  }

  function drawRateChart(canvas, bins, labels) {
    const ctx = canvas.getContext('2d');
    const w = canvas.width;
    const h = canvas.height;
    ctx.clearRect(0,0,w,h);

    const pad = 28;
    const gw = w - pad*2;
    const gh = h - pad*2;

    const maxV = Math.max(1, ...bins);

    ctx.globalAlpha = 0.18;
    ctx.lineWidth = 1;
    for (let i=0;i<=4;i++) {
      const y = pad + (gh * i / 4);
      ctx.beginPath();
      ctx.moveTo(pad, y);
      ctx.lineTo(pad+gw, y);
      ctx.stroke();
    }
    ctx.globalAlpha = 1;

    const barW = gw / Math.max(1, bins.length);

    const col = getComputedStyle(canvas).color || '#ffffff';
    ctx.fillStyle = col;
    ctx.globalAlpha = 0.65;
    for (let i=0;i<bins.length;i++) {
      const v = bins[i];
      const bh = (v / maxV) * gh;
      const x = pad + i*barW;
      const y = pad + gh - bh;
      ctx.fillRect(x + 1, y, Math.max(1, barW - 2), bh);
    }
    ctx.globalAlpha = 1;

    ctx.font = '12px system-ui, -apple-system, Segoe UI, Roboto, Arial';
    ctx.globalAlpha = 0.8;
    ctx.fillText('QSOs/min', pad, 16);

    const step = Math.ceil(labels.length / 6);
    for (let i=0;i<labels.length;i+=step) {
      const x = pad + i*barW;
      ctx.fillText(labels[i], x, h - 8);
    }
    ctx.globalAlpha = 1;
  }

  async function run() {
    const dash = document.querySelector('.qsoDash');
    if (!dash) return;

    const aUrl = dash.getAttribute('data-adif-a');
    const bUrl = dash.getAttribute('data-adif-b');

    const errBox = dash.querySelector('[data-qso-error]');
    const setErr = (msg) => {
      if (!errBox) return;
      errBox.hidden = false;
      errBox.textContent = msg;
    };

    let recs = [];
    try {
      const aRes = await fetch(aUrl);
      const bRes = await fetch(bUrl);
      if (!aRes.ok || !bRes.ok) throw new Error('ADIF fetch failed');
      const aTxt = await aRes.text();
      const bTxt = await bRes.text();
      recs = parseAdif(aTxt).concat(parseAdif(bTxt));
    } catch (e) {
      setErr('Could not load ADIF logs. Place the two .adi files in /public/logs/ using the filenames shown in the note above.');
      return;
    }

    const qsos = recs
      .map((r) => {
        const dt = toUtcDate(r);
        return {
          dt,
          utc: dt ? fmtUTC(dt) : '',
          call: (r.CALL || '').trim().toUpperCase(),
          band: bandLabel(r) || '',
          mode: modeLabel(r) || '',
          rstS: (r.RST_SENT || '').trim(),
          rstR: (r.RST_RCVD || '').trim(),
          grid: (r.GRIDSQUARE || '').trim().toUpperCase(),
          ref: refLabel(r),
        };
      })
      .filter((q) => q.call || q.dt)
      .sort((x,y) => {
        if (!x.dt && !y.dt) return 0;
        if (!x.dt) return 1;
        if (!y.dt) return -1;
        return x.dt - y.dt;
      });

    const total = qsos.length;
    const first = qsos.find(q => q.dt)?.dt || null;
    const last = qsos.slice().reverse().find(q => q.dt)?.dt || null;
    const duration = (first && last) ? (last - first) : 0;

    const bandsSet = new Set(qsos.map(q => q.band).filter(Boolean));
    const modesSet = new Set(qsos.map(q => q.mode).filter(Boolean));

    const kpi = (key) => dash.querySelector('[data-kpi="' + key + '"]');
    if (kpi('total')) kpi('total').textContent = String(total);
    if (kpi('duration')) kpi('duration').textContent = first && last ? fmtDur(duration) : '—';
    if (kpi('first')) kpi('first').textContent = first ? fmtUTC(first) : '—';
    if (kpi('last')) kpi('last').textContent = last ? fmtUTC(last) : '—';
    if (kpi('bands')) kpi('bands').textContent = bandsSet.size ? Array.from(bandsSet).join(', ') : '—';
    if (kpi('modes')) kpi('modes').textContent = modesSet.size ? Array.from(modesSet).join(', ') : '—';

    const bandCounts = {};
    for (const q of qsos) {
      if (!q.band) continue;
      bandCounts[q.band] = (bandCounts[q.band] || 0) + 1;
    }
    const bandsSorted = Object.entries(bandCounts).sort((a,b) => b[1]-a[1]);
    const bandTbody = dash.querySelector('[data-band-table]');
    if (bandTbody) {
      bandTbody.innerHTML = '';
      for (const [band, count] of bandsSorted) {
        const pct = total ? Math.round((count/total)*100) : 0;
        const tr = document.createElement('tr');
        tr.innerHTML = '<td>' + band + '</td><td>' + count + '</td><td>' + pct + '%</td>';
        bandTbody.appendChild(tr);
      }
      if (!bandsSorted.length) {
        const tr = document.createElement('tr');
        tr.innerHTML = '<td colspan="3">No band data found in the ADIF.</td>';
        bandTbody.appendChild(tr);
      }
    }

    const chart = document.getElementById('qsoRateChart');
    if (chart && first && last) {
      const startMs = first.getTime();
      const endMs = last.getTime();
      const binMs = 60 * 1000;
      const nBins = Math.max(1, Math.ceil((endMs - startMs + 1) / binMs));
      const bins = new Array(nBins).fill(0);
      for (const q of qsos) {
        if (!q.dt) continue;
        const idx = Math.min(nBins - 1, Math.max(0, Math.floor((q.dt.getTime() - startMs) / binMs)));
        bins[idx] += 1;
      }
      const labels = bins.map((_, i) => {
        const d = new Date(startMs + i*binMs);
        return pad2(d.getUTCHours()) + ':' + pad2(d.getUTCMinutes());
      });
      drawRateChart(chart, bins, labels);

      const peak = Math.max(...bins);
      const avg = total / nBins;
      const meta = dash.querySelector('[data-rate-meta]');
      if (meta) meta.textContent = 'Peak: ' + peak + ' QSOs/min • Avg: ' + avg.toFixed(2) + ' QSOs/min • Bins: ' + nBins;
    }

    const tableBody = dash.querySelector('[data-qso-table]');
    const filterInput = dash.querySelector('[data-qso-filter]');
    const bandSel = dash.querySelector('[data-qso-band]');
    const modeSel = dash.querySelector('[data-qso-mode]');

    const uniq = (arr) => Array.from(new Set(arr)).filter(Boolean).sort();
    if (bandSel) {
      for (const b of uniq(qsos.map(q => q.band))) {
        const opt = document.createElement('option');
        opt.value = b;
        opt.textContent = b;
        bandSel.appendChild(opt);
      }
    }
    if (modeSel) {
      for (const m of uniq(qsos.map(q => q.mode))) {
        const opt = document.createElement('option');
        opt.value = m;
        opt.textContent = m;
        modeSel.appendChild(opt);
      }
    }

    const renderRows = () => {
      if (!tableBody) return;
      const q = (filterInput && filterInput.value ? filterInput.value : '').trim().toUpperCase();
      const b = (bandSel && bandSel.value ? bandSel.value : '').trim().toLowerCase();
      const m = (modeSel && modeSel.value ? modeSel.value : '').trim().toUpperCase();

      const rows = qsos.filter((x) => {
        if (b && x.band !== b) return false;
        if (m && x.mode !== m) return false;
        if (!q) return true;
        const hay = (x.utc + ' ' + x.call + ' ' + x.band + ' ' + x.mode + ' ' + x.grid + ' ' + x.ref).toUpperCase();
        return hay.includes(q);
      });

      tableBody.innerHTML = '';
      for (const r of rows) {
        const tr = document.createElement('tr');
        tr.innerHTML =
          '<td>' + (r.utc || '—') + '</td>' +
          '<td>' + (r.call || '—') + '</td>' +
          '<td>' + (r.band || '—') + '</td>' +
          '<td>' + (r.mode || '—') + '</td>' +
          '<td>' + (r.rstS || '—') + '</td>' +
          '<td>' + (r.rstR || '—') + '</td>' +
          '<td>' + (r.grid || '—') + '</td>' +
          '<td>' + (r.ref || '—') + '</td>';
        tableBody.appendChild(tr);
      }

      if (!rows.length) {
        const tr = document.createElement('tr');
        tr.innerHTML = '<td colspan="8">No QSOs match the current filters.</td>';
        tableBody.appendChild(tr);
      }
    };

    if (filterInput) filterInput.addEventListener('input', renderRows);
    if (bandSel) bandSel.addEventListener('change', renderRows);
    if (modeSel) modeSel.addEventListener('change', renderRows);
    renderRows();
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', run);
  } else {
    run();
  }
})();
`}</script>